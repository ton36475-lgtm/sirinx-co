# SIRINX Pre-Server System Review

This is the canonical current entrypoint for the latest pre-server review state.

Use the detailed dated review record in:

- `docs/migration/PRE_SERVER_SYSTEM_REVIEW_2026-04-24.md`

Required validation lanes:

- real file validation
- JSON schema validation
- locked fact validation
- pre-deploy validation
- local runtime smoke validation
- handoff bundle validation
- overlap audit
- final bundle rebuild

The server team must not treat this review as deployment authorization.
