# SIRINX incident evidence — 20 September 2026 (Asia/Bangkok)

## Status

Release blocked by real media/API failures and unavailable authenticated Cloudflare/device connections.
The previous build-only checks were insufficient to establish live-site recovery. PR #10 is still a draft; no main merge or Cloudflare deployment has been performed by this recovery session.

## Executed evidence

Source tested: cf5cda9b3c08e3f610dfb90e7c286298bac31c1a.
CI: https://github.com/ton36475-lgtm/sirinx-co/actions/runs/35461569901
Browser job: 105946259759.
UTC observation: 2026-09-19 18:34–18:36, corresponding to Bangkok 2026-09-20 01:34–01:36.
Artifact: https://github.com/ton36475-lgtm/sirinx-co/actions/runs/35461569901/artifacts/10588929531
Artifact contains 8 browser screenshots plus browser-audit.json, 1,735,631 bytes; ZIP SHA-256:
1022569cd8c3814f1ea451b5ce3989e536092d42d56a3f25a50cdef9318762a0

- Frozen installation, application typecheck, 44 client tests, 11 standalone recovery tests and the static build passed.
- Build generated 94 SEO routes (77 province routes); static preflight checked 221 files.
- All 28 sampled browser visits rendered React content without an uncaught page exception: seven routes, desktop/mobile, local build/live site.
- Asset checks failed in 11 of 14 visits in each environment. This makes the website CI job FAIL, intentionally; it must not be reported as green.
- Live desktop Projects: 37 broken images out of 41 with selected sources at the sampled time. Lazy-loading, viewport and wait duration affect counts; this is not a complete inventory.
- Original carport-wide-1_30e3af4c.jpeg on the existing CloudFront origin returned HTTP 403, application/xml, server AmazonS3.
- The corresponding /cdn-cgi/image/ URL also returned 403. Disabling transformations does NOT restore an inaccessible original.
- GET /api/trpc/auth.me?batch=1 returned HTTP 200 text/html containing the home-page title, not JSON. API route/backend repair is required independently of static deployment.
- Existing local solar-carport-hero-960.jpg returned 200 image/jpeg. Do not claim every image has been deleted.
- Initial live JS/CSS assets returned 200 with expected MIME types. Their filenames differ from the recovered build; authenticated deployment-source mapping is still needed.
- Asset Cache-Control combines max-age=0 and max-age=31536000 through overlapping rules. This was observed but is not established as the cause of the image failure.

## Limits and causal interpretation

The immediate missing-image mechanism is an unavailable origin response, reproduced in both source preview and production. The account-side reason for the 403 (object permissions, origin policy, missing object hidden by policy, or another upstream change) is NOT known. Do not attribute it to Supabase, a particular person, or a Cloudflare settings change without evidence.
The unmerged repair cannot be treated as the proven deployed cause; a direct CLI deployment could only be established from account deployment records.
The local static server is not Cloudflare edge emulation. Browser non-GET/HEAD requests were blocked; no form, chat, login or database write was sent. HTTP checks are unauthenticated and Chromium-only.

## Added mitigation

apiResponse.ts rejects a non-JSON API response before tRPC parsing, preserving legitimate JSON error responses and unread bodies. Its generic error is recognized by the existing contact fallback rather than reporting a submitted lead. It neither creates a backend nor repairs production routing. The new tests need verification at the final PR head; the earlier run predates this mitigation.

## Required restoration

1. Recover original owned media or restore the legitimate source's permissions. Do not publish unrelated project photos as replacements, generate fictitious proof, or bypass access controls.
2. Verify Cloudflare account/zone/project/source/production branch, existing Functions and Worker routes, and the rollback deployment ID.
3. Restore /api/trpc to the real backend and test against an isolated test database. Do not create a fake always-success endpoint.
4. Resolve image errors and run the full browser gate. Then deploy a non-production Cloudflare preview and verify actual edge headers, routes and integration behavior.
5. Promote only the tested artifact and perform post-deploy checks. Do not merge solely because unit tests pass.

## Other requested work

Mac /Users/sirinx was not connected; Windows D:/ node AIAgent was offline; SentinelX returned no hosts/integrations. Cloudflare and custom GhostClaw/Blender connectors could not be discovered. No device pairing, 47-Ronins dispatch, GPU driver installation, tunnel creation or Blender execution occurred.
Keep those lanes separate from incident repair. Preserve the existing Mac authority and four-worker limit; perform NVIDIA/AMD hardware compatibility checks after website recovery. Apple Silicon uses Metal/MLX rather than local CUDA. Keep Blender's raw socket private; inspect the existing custom bridge/port before changing it. See the separately delivered cross-node research and preflight pack.

Primary Cloudflare header reference: https://developers.cloudflare.com/pages/configuration/headers/
