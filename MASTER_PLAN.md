# SIRINX MASTER PLAN — แผนงานหลักเดียวของระบบ

The one canonical plan. Every other plan document is an appendix of
this file (see the doc map in `.claude/skills/sirinx-master-plan/`).
Update the status column here in the same commit as the work.

Goal spec: **one Rust monorepo powering www.sirinx.co, operated by an
autonomous-but-gated 47 Ronin agent mesh across Mac/PC/cloud nodes,
with knowledge, work, and routing shared through one backbone.**

## A. DONE (merged to main or on the working branch)

| # | Deliverable | Proof |
| --- | --- | --- |
| A1 | Cargo workspace, 8 crates (core/roi/agents/autoloop/store/a2a/web/control) | 61 Rust tests |
| A2 | Thaimart landing + lead/ROI API, brand-safe guards | `sirinx-web` tests |
| A3 | Supabase persistence: web_leads / web_analytics_events / web_pending_work (+pg_notify), RLS | migrations 0001-0002, live |
| A4 | Hermes Command Center imported + 120 Node tests green from root | `apps/dev-dashboard`, `services/dev-control-api` |
| A5 | Release gates hold-by-default + ticket opening + bearer auth + /metrics | `sirinx-control` |
| A6 | A2A mesh + OmniRoute, capabilities from 50 skills | `sirinx-a2a`, historical local in-process route smoke only; no authenticated external peer |
| A7 | Brain @ Edge: D1 schema live (APAC) + brain-sync worker written | `infra/cloudflare/brain-sync-worker` |
| A8 | CI workflow + governance phase advance; Docker deploy path (gated) | `.github/workflows/ci.yml`, `Dockerfile` |
| A9 | Ronin org consolidated: 6 lead sub-agent files (L1–L5 + Kai) + 47-role roster (schema + JS descriptors) + 4 coded Rust agents + 50 skills + MIT license | `.claude/`, `crates/sirinx-agents`, `LICENSE` |
| A10 | **Ronin lead pipeline live**: lead POST → L1→L2 ROI scoring→L3 decision→L4 auto-enqueues follow-up work | `sirinx-agents::ronin`, web test |
| A11 | Postman collection "SIRINX Platform API" (all 4 surfaces) | collection `e6b5fcae…` |
| A12 | PR #6 merged to main | GitHub |
| A13 | **Durable release gates**: typed core model + `web_control_gates` + Store-backed control hydration/write-through | migration 0003; memory/store/reload tests |
| A14 | **Self-learning recovery loop**: bounded failure events + deduplicated structured lessons + gate-preserving retries | migration 0004; failure→lesson→guided retry and budget tests |
| A15 | **GHOSTCLAW · Hermes V3 Command Center**: bilingual, read-only, owner-only Sites briefing deployed | Sites version `appgver_f10b41d7f3cc81918b9f95889a8aa2cd`; PR #8; go-live receipt #7 |
| A16 | **B4 done — Ronin roster expansion**: 3 new L1 scanners (FB group 02, LINE OA 03, partner referral 04) + 2 new L2 scorers (SME 18, 24/7 ops 19) feeding the same pipeline; PDPA-conservative consent defaults | `sirinx-agents::{fb_group,line_events,partner_referral,scorer_sme,scorer_round_the_clock}`; 31 new tests; full chain green 2026-07-19 |
| A17 | **Fail-closed Telegram/A2A Node control plane**: fixed destination, Bearer + bounded idempotency, fresh Postgres gate evidence, CenterBrain readiness, 15 unique agents / 15 configured OmniRoute lanes, Claude Cowork + Kimi surface truth, and one bounded OpenCode write receipt | `services/{telegram-command-bot,dev-control-api}` + `sirinx-control {/ready,/api/gates}` + `reports/runtime/opencode-handshake-20260720-001`; no configured lane is runtime activation evidence; paid-provider activation is unsupported until future `PROVIDER_CALL` + `provider_call`, not C5 |
| A18 | **47-role passive operating contract + cross-language registry projection**: all 47 role cards plus Kai reviewed, unique duties/cards, immutable fail-closed Rust/JavaScript projections from one crate-contained JSON artifact, `PRODUCTION_HOLD` truth index, bounded swarm/background/harness contract v1.1 with every schedule disabled, closed task/lease/approval/receipt schemas, and local negative invariants | `PRODUCTION.md`; `crates/sirinx-agents/{data,src/role_registry.rs}`; `services/dev-control-api/src/ronin-role-registry.mjs`; `docs/{agents/ronin,agent-runtime}`; `schemas/agent-runtime`; 78 `sirinx-agents` Rust + 36 relevant Node tests; human card review; Cargo package file-list proof |
| A19 | **Durable agent-runtime P2.1 static candidate**: migrations 0005–0006 define 13 forced-RLS tables plus a separate NOLOGIN owner/app authority path; a dedicated non-migrating Postgres runtime store attests exact membership, ownership, 13 policies, five-table column capabilities, three sequences, withheld groundwork tables, and zero effective `anon`/`authenticated`/`service_role` access; typed Memory/Postgres task/run/event/lease/receipt logic retains CAS, DB-time leases, one writer, independent PASS, append-only hashes, and `EFFECT_UNKNOWN` | `crates/sirinx-{core,store}` + migrations 0005–0006 + Phase-A disposable harness + `reports/runtime/agent-runtime-p2-durable-local-20260720.md`; independent verdict `STATIC_P2_1_VERIFIED`; final files still require compile/tests, Phase-B probes, admitted live disposable Postgres, and explicit service wiring |
| A20 | **Protected-read and CodexBridge static repair candidate**: Ronin status reads neither Hermes profile files nor profile-tree metadata; active counts require redacted CWD plus profile/CWD-bound fresh running/handshake evidence accepted by an injected attestation verifier; cross-profile replay is rejected; the Rust/Node bridge has a closed status-only input and strictly validated read-only output and rejects legacy action-like modes | `services/dev-control-api/src/{agent-team,codex-autoloop}.mjs` + tests; `crates/sirinx-autoloop/src/{codex_bridge,lib}.rs`; Node syntax and Rust formatting pass; functional tests remain resource-blocked |
| A21 | **MCP/A2A connection-admission static candidate**: one closed 16-entry registry covers Cloudflare MCP/portal, Codex, Claude/Cowork, Kimi, Hermes, LINE MCP, Telegram/LINE transports, and four A2A peers; every entry is disabled, deny-all, and runtime-unverified; a local loader rejects enablement, insecure endpoints, duplicate IDs, unknown fields, and effects without matching proposed tickets | `schemas/agent-runtime/mcp-connection-registry.v1.schema.json`; `config/agent-runtime/mcp-connections.plan-only.v1.json`; `services/dev-control-api/src/mcp-connection-plan.mjs`; `docs/agent-runtime/MCP_A2A_CONNECTION_PLAN.md`; JSON parse and static-loader validation pass; no client config, OAuth, network, install, server start, Cloudflare mutation, A2A egress, or send occurred |
| A22 | **Resource-recovery read-only admission packet**: measured the exact checkout filesystem at 10.114 GiB free, inventoried literal build/cache candidates, separated the absolute 15 GiB floor from a conservative 20 GiB full-chain target, defined permanent exclusions and one-target maker/checker receipts, and independently rejected execution without an exact cleanup grant | `docs/agent-runtime/RESOURCE_RECOVERY_ADMISSION.md`; `reports/runtime/resource-recovery-admission-plan-20260720.md`; no deletion, Trash move, package prune, model/Docker/database mutation, install, or external effect occurred |
| A23 | **Resource-cleanup v2 static preflight candidate**: closed plan/approval/target-manifest/process-evidence schemas bind one exact generated target, canonical repository, absolute Cargo binary, explicit target dir, and non-inherited environment; a read-only loader recomputes domain-separated plan/scope/grant/entry/process digests, rejects path/exclusion overlap, stale or reordered evidence, manifest/process drift, unsafe operations, grant overlife, packet overbounds, and reclaim over-credit, while unconditionally reporting no authority, no admission, held circuit, no replay protection, no executor, and no approval consumption | `reports/runtime/resource-cleanup-v2-static-preflight-20260720.md`; final independent verdict `VERIFIED` for HOLD-only scope; 10 focused tests pass; no migration, server route, process probe, cleanup, command, network call, or external effect |
| A24 | **Resource-cleanup B12 HOLD-only executor-admission candidate**: four closed schemas and a pure validator bind the launcher symlink chain, selected Cargo binary and exact revision, user-owned path ancestry, post-grant target/process/repository/lease/resource evidence, PREPARED-only attempts, and context-bound structural post-action artifacts; caller-authored PASS without full pre/post/checker contexts is rejected, while output remains unconditionally no authority/replay/executor/approval consumption/dispatch | `reports/runtime/resource-cleanup-admission-static-20260720.md`; `docs/agent-runtime/RESOURCE_CLEANUP_AUTHORITY_KERNEL.md`; final independent verdict `VERIFIED` for HOLD-only/no-dispatch scope; 9 focused tests pass; migration 0007 is deliberately deferred and no collector, cleanup, process, database, network, route, or external effect exists |
| A25 | **Draft 2020-12 cleanup-schema instance parity**: Ajv 8.20.0 plus formats 3.0.1 are pinned in npm/pnpm metadata; strict compilation registers external target/process references and validates positive fixtures for all four A24 schemas while rejecting open objects, malformed time, forbidden REQUESTING, and false PASS | same A24 evidence report; focused file remains 9/9 with the schema engine enabled; `npm ci --dry-run` accepted the lock offline with scripts disabled; no package install, collector, route, database, network, process, or effect occurred |
| A26 | **B11 Connection Evidence Admission Preview + raw-readiness quarantine**: two closed Draft 2020-12 schemas and a pure validator bind the review-pinned disabled plan, peer/principal, exact component revision/license/provenance, canonical endpoint origin, AgentCard/protocol/capability/data ceiling, collector, freshness, and task/run/lease/receipt digests; output is only `EVIDENCE_VALIDATED_NOT_ADMITTED`, with caller context/clock/DNS/replay authority explicitly absent; OmniRoute/card compatibility inputs are retained only as `reported*` and can no longer verify endpoints, admit registration, make a handshake ready, or enable MCP | `reports/runtime/connection-evidence-admission-preview-20260720.md`; independent safety/red-team verdicts `VERIFIED`, with schema-review gaps remediated; 81 focused tests pass; runtime-card projection 1.2 and OmniRoute 1.4; no route, collector, filesystem/network/process/env/database/provider/message effect, connection, or external action |
| A27 | **B10.0 Shared Authority Contract Manifest + pure HOLD-only preview**: a closed generic approval-receipt v2 structural contract is bound to an ordered, review-pinned 13-row action/circuit manifest and the A26 connection-plan digest; a portable length-framed Rust/Node wire vector freezes the receipt digest and `effectKey` is exactly `circuitName:grantId`; Rust owns semantic validation and returns only an opaque `CONTRACT_VALIDATED_NOT_AUTHORIZED` preview. Every circuit is `HOLD`, every executor/effect route is absent, LINE and customer messaging are unbound, and Telegram is pinned to its exact transport, target prefix, and scope schema | `reports/runtime/shared-authority-contract-manifest-20260720.md`; 16 focused Rust tests + 5 Node schema/wire contract tests; no migration 0007, route, store, clock authority, approval consumption, claim, executor, process/network/database/provider/message I/O, or external effect |
| A28 | **B10.1 designated compatibility-surface static packet**: one exact closed `DURABLE_AUTHORITY_UNAVAILABLE` fixture/schema and a review-pinned 39-surface inventory account for Rust action/A2A/queue/readiness (including the web lead follow-up queue), Node A2A/OmniRoute/CenterBrain/Codex-bridge-to-Telegram, Telegram HTTP/direct/helper/readiness/injected-gate seams, and the stale Telegram live runbook; the local validator binds 28 working-tree source/test/store/migration/doc hashes, independently discovers scoped Rust method/path/handler bindings plus Node/Telegram method/path bindings and exports, rejects duplicate JSON keys, applies strict schemas, and recomputes the A27 domain digest while every runtime/effect/readiness flag remains false | `reports/runtime/b10-compatibility-surface-inventory-20260721.md`; 17 focused Node tests; independent packet/schema reviews `CLEAN` and evidence verdict `VERIFIED` for static accounting only—runtime quarantine, migration 0007, durable authority, MCP/A2A live sync, provider/send/queue effects, and production readiness remain absent |
| A29 | **External component research/intake freeze**: 26 publisher-source entries cover Cloudflare/MCP/A2A, existing Codex/Hermes/Kimi/Axum/SQLx, QA/browser, Telegram/LINE alternatives, and local/hosted models under one strict MIT/Apache-only schema; the local validator binds exact catalog/schema bytes, closed publisher/source/disposition membership, duplicate-key rejection, resource/overlap/version/license quarantines, four held install waves, and false clone/install/run/connect/effect claims | `docs/agent-runtime/EXTERNAL_COMPONENT_INTAKE.md`; `config/agent-runtime/external-components.research-only.v1.json`; `reports/runtime/external-component-intake-20260721.md`; 11 focused Node tests pass; no clone, install, lifecycle script, model download/invocation, config/secret read, OAuth, MCP/A2A connection, provider call, Cloudflare mutation, message, push, merge, or deploy occurred; 14,268,520 KiB free remains below the 15 GiB floor |
| A30 | **B13 Wave 0 existing-component provenance freeze**: six already-present components (Codex, Hermes, Kimi, Axum, SQLx, local Qwen) are reconciled into one closed read-only packet whose validator pins the A29 parent, capture-time dirty-worktree metadata, exact `Cargo.lock` bytes, all top-level/component semantics, 26 allowlisted local paths, symlink/canonical target/inode/full-mode/size/full-file SHA-256, and seven cached crates.io archives; bounded same-descriptor reads reject path/target swaps before reading, hard-link aliases, growth/truncation, non-regular files, path injection, protected-boundary narrowing, checksum/order/proof promotion, deep/wide/Proxy/accessor graphs, and all runtime/admission claims | `docs/agent-runtime/EXISTING_COMPONENT_PROVENANCE.md`; `config/agent-runtime/existing-components.provenance.v1.json`; `reports/runtime/existing-component-provenance-20260721.md`; latest-byte code review `CLEAN`, adversarial red-team `CLEAN`, evidence verdict `VERIFIED` for static local scope, and 14 focused Node tests pass; result is `PROVENANCE_VALIDATED_NOT_ADMITTED`, `0/6` upstream identities bound, Hermes remains dirty editable, `/opt/homebrew/bin/kimi` is a test stub, SQLx publisher metadata disagrees with the parent catalog, Qwen remains upstream-unbound, and the 14,007,668 KiB capture keeps resource admission on HOLD; branch/HEAD/Hermes Git/disk values require fresh operational read-back, historical no-effect claims remain attestations, and no runtime/production status is proven |
| A31 | **B12 C01 proposal-only review request**: a closed Draft 2020-12 schema, pure validator, and one-hour digest-bound packet freeze pre-collected local observations for the exact Cargo `target`; the contract pins A23 plan/operation/exclusion digests, task/run and pairwise-distinct proposed principals, exact repository/tool paths, metadata-only target counts including 5,492 complete internal hard-link groups, limited target-scoped `lsof`, mutable unbound `stable` toolchain evidence, proposed zero-growth arithmetic, and 22 mandatory blockers while returning a distinct deeply frozen `COLLECTED_NOT_APPROVED` clone with every authority/effect flag false | `docs/agent-runtime/RESOURCE_CLEANUP_REVIEW_REQUEST.md`; `config/agent-runtime/resource-cleanup.review-request.v1.json`; `reports/runtime/resource-cleanup-review-request-20260721.md`; focused 10/10, `npm run check`, and control 326/326 pass; latest-byte code review `CLEAN`; independent evidence `VERIFIED` for recorded proposal scope only. The capture nominally projected 16,751,204 KiB, but the final 04:29 read-back found 11,818,956 KiB free and C01 then projected only 14,944,944 KiB—783,696 KiB below 15 GiB. No approval collector, grant, replay ledger, executor, cleanup, Cargo command, build, install, connection, send, cloud/database mutation, push, merge, or deploy exists |
| A32 | **B12 resource-recovery bootstrap comparison**: an immutable comparison-only packet pins A22/A23/A31 bytes, exact branch/HEAD, capture/expiry/IDs/digest, four granular candidates (C01/C02a/C05a/C05b), wholesale-ineligible npm/Go/pnpm roots, keep-by-default pnpm-v11/Cargo registry, zero guaranteed APFS reclaim, single-target/stop-and-remeasure policy, ten blockers, and all proof/authority/effect flags false. None of the four candidates alone nominally reaches 15 GiB, so the result can only be `BOOTSTRAP_REVIEW_BLOCKED` | `docs/agent-runtime/RESOURCE_RECOVERY_BOOTSTRAP_REVIEW.md`; `config/agent-runtime/resource-recovery.bootstrap-review.v1.json`; `reports/runtime/resource-recovery-bootstrap-review-20260721.md`; focused 18/18, `npm run check`, and control 344/344 pass. Two initial P2 findings (timestamp reseal and missing-config false green) are closed; latest-byte review `CLEAN`, evidence `VERIFIED` for the exact static scope. The 04:59 read-back was 11,812,640 KiB free and C01 remained 790,012 KiB below 15 GiB. No cleanup, grant, command, install, build, connection, database/cloud mutation, send, push, merge, or deploy occurred |
| A33 | **Migration-0007 authority-semantics freeze**: a closed Draft 2020-12 packet and pure latest-byte validator resolve the one-vs-13 drift by requiring one shared additive kernel to reuse 0005 tickets/grants/outbox, install the exact ordered A27 13-definition registry plus 13 version-one `HOLD` circuit rows, and seed zero tickets, grants, attestations, admissions, outbox claims, LOGIN roles, executor registrations, routes, or open circuits. `RESOURCE_CLEANUP` is exactly one registry member, action scope uses one generic v2 intent binding, 0006 app denial remains exact, and 17 external bootstrap bindings prevent the new kernel from authorizing its own install | `docs/agent-runtime/EFFECT_AUTHORITY_MIGRATION_SEMANTICS.md`; `config/agent-runtime/effect-authority-migration.semantics.plan-only.v1.json`; `reports/runtime/effect-authority-migration-semantics-20260721.md`; packet digest `ccb4a5afc0a2ee33114859dd777e4ebcd758f03abf92a103f075405c9324fcea`; post-review focused 12/12, code review `CLEAN`, evidence `VERIFIED` for static working-tree scope. Migration 0007 remains absent/deferred, every authority/effect flag is false, and the final read-back was 9,938,592 KiB free. No SQL, role, runtime, MCP/A2A, message, cloud/database, push, merge, or deploy effect occurred |

| A34 | **A2A registry expansion + blocked-goal regex repair**: Antigravity (AGY) registered as the 16th sync agent with OmniRoute lane/routes/tmux plan and `agy` card alias (`registered-unverified-runtime`, no execution principal, no admission); grouped-alternation fixes in dangerous-goal rules remove false blocks (e.g. goal word "linear" no longer trips `customer_message_send`); OmniRoute 1.5; team-runtime-bridge and ai-team-pairing projections extended in the same working tree; six count-coupled test files updated | `services/dev-control-api/src/{a2a-sync,a2a-omniroute}.mjs` + test files; control suite 356/356 green 2026-07-21; review `reports/agent-inventory/A2A_LIVESYNC_SYSTEMS_REVIEW_20260721.md` + jobs board `AGENT_JOBS_INTEGRATION_BOARD_20260721.md`; no runtime admission, lane activation, provider call, send, push, or deploy occurred |

## B. QUEUED — engineering (in priority order)

| # | Work item | Definition of done |
| --- | --- | --- |
| B3 | R2-parity: port remaining routes from `automation-system-backend` + `sirinx` api-gateway into `sirinx-web` | route inventory diff = empty |
| B5 | Node bridge: quarantine the hermes-os A2A (:9000) card as candidate evidence, pass B11 connection-evidence admission, then register through an authenticated Rust trust adapter; Obsidian vault sync remains separately gated (A17 supplies only the local evidence plane) | candidate card cannot expose pending work or become runtime truth before identity/revision/digest/freshness/task/lease/receipt validation; later admitted cards visible via `/api/a2a/route`; notes flow to D1 only under a separate external-effect ticket |
| B6 | R4 web consolidation (marketing/chokma/dashboard apps onto sirinx-web API) | per-app cutover |
| B7 | GhostClaw implementation (contract-first per INTEGRATION_MAP) — **blocked: rotate+purge committed Android keystore first** | quarantine review passed |
| B8 | R5 long tail: mobile apps on central API, archive legacy repos | REPO_AUDIT map all "done" |
| B9 | 47-role runtime manager: restore resource admission, compile/test the final P2.1 files, run migrations 0001–0006 from empty and prior disposable states through split migration/runtime identities, complete Phase-B RLS/concurrency/receipt/rollback probes, then enable one local dry-run maker/checker pilot | post-remediation Rust checks + full disposable Postgres state/RLS suite + one receipt-bound local pilot + zero external effects |
| B10 | A27 freezes the closed approval-receipt v2 structural contract and complete 13-row HOLD-only action/circuit map; A28 freezes the exact shared refusal shape across 39 designated compatibility surfaces; A33 now freezes the persistence/bootstrap decision as exactly 13 definitions + 13 held circuits + zero authority-bearing seeds, reusing 0005 tickets/grants/outbox and forbidding self-authorized installation. None changes runtime. After resource admission, implement the A28 refusal before all direct Rust action/A2A/queue, Node live A2A/OmniRoute/Hermes-probe, and Telegram HTTP/direct effect seams; suppress only the web lead's derived follow-up enqueue, force legacy authority/readiness projections false, and retire the stale live-send runbook. Then implement the single coordinated additive migration 0007 under A33 for principal attestations, human-only grants, atomic single-use effect claims, pre-effect `REQUESTING`, per-executor RLS, exact version-aware inventory, and redacted operator snapshots. Provider/model catalogs and routes consume this kernel rather than creating a second approval ledger | cross-language 503 refusal/schema parity + zero-seam invocation tests + exact A33 registry/bootstrap parity + verified license/provenance/revision/resource receipts + disposable Postgres/RLS/idempotency/race/crash/restore suite + one network-denied local Qwen pilot; every cleanup, install, connector activation, paid call, A2A egress, send, queue/cloud mutation, production migration, and deploy remains a separate ticket; `local_inference` remains a separate resource/model admission lane and gains no external-effect authority from A27/A28/A33 |
| B11 | A21+A26 complete the disabled registry and HOLD-only evidence preview. Next consume B10 managed authority for an authoritative plan/context source, trusted clock, replay/uniqueness ledger, authenticated endpoint/AgentCard verification, and per-effect admission; do not wire A26 into OmniRoute readiness or a server route | disposable Postgres/RLS/replay/race/restore proof plus exact revision/license/provenance/resource receipts, authentication, tool allowlist, A2A trust/TCK and fresh heartbeat; only then one separately ticketed connector canary, while MCP/A2A/send flags remain false by default |
| B12 | Resource Recovery Executor: A24–A25 complete HOLD-only action-time shapes, A31 freezes one expired C01 proposal, A32 proves no reviewed granular candidate alone reaches 15 GiB while every guaranteed reclaim remains zero, and A33 resolves the migration-0007 registry decision without creating authority. The resource/bootstrap cycle remains: keep wholesale roots ineligible and require a new one-target observation/grant after every stop-and-remeasure. After resource admission, implement A28 runtime refusal, prove the A33 shared Authority Kernel on disposable Postgres, then add attested human `resource_cleanup` persistence/RLS and the narrow Rust descriptor-relative, no-follow, two-pass collector before any executor; align the harness with a reviewed growth margin, refuse `KEEP=1` in capacity recovery, and retire legacy blanket auto-clean language | coordinated shared-kernel Postgres/RLS/replay/crash proof + all 17 external bootstrap bindings + exact fresh one-target dry-run packet + complete Git/target/process/tool identities + independently reviewed APFS-safe minimum reclaim + one separately human-approved recoverable target + unchanged exclusions + post-action free space at the operation-specific threshold; A23–A25/A31–A33 are not authority, aggregate portfolio arithmetic is not reclaim credit, and no cleanup is inherited from `APPROVE_IMPLEMENTATION` |
| B13 | External component exact-revision admission: A30 completes the read-only Wave 0 local-byte reconciliation for Codex/Hermes/Kimi/Axum/SQLx/Qwen, but admits none—upstream/install/runtime proof is still `0/6`, Hermes is dirty editable, the alternate Homebrew-path Kimi is a stub, SQLx ownership is discrepant, and Qwen lacks upstream/conversion attestation. After B10/B11 authority and resource recovery, resolve one discrepancy at a time before admitting at most one narrow component/install wave; the first new-runtime candidate remains a stateless documentation-only MCP local preview, while A2A v1 remains blocked on stable SDK/TCK alignment and GLM-5.2/Kimi K3 remain local rejects | exact immutable upstream revision/artifact/license/dependency/lifecycle/footprint/rollback receipt + fresh resource receipt + separately ticketed install/connector/effect lane + independent checker/read-back; never add a duplicate orchestrator, broad Cloudflare/LINE mutation MCP, second Telegram framework, or model artifact merely because local bytes or a permissive license exist |

B3 static inventory note (2026-07-21): the frozen sources contain 25
`automation-system-backend` gateway declarations plus 29 `sirinx` tRPC/OAuth
operations. The target has 10 Axum registrations and exact response-contract
parity is currently 0/54; business-purpose overlap is not counted as a port.
The first bounded slice is the published-only `blog.list`/`blog.getBySlug`
pair, but implementation/build/migration work remains resource-held below the
15 GiB workload floor. Migration 0007 stays reserved for B10. Full inventory,
quarantine decisions, ownership, validation, and rollback are in
`docs/agent-runtime/B3_ROUTE_PARITY_PLAN.md`.
The closed 54-row manifest and no-dependency validator now return only
`STATIC_INVENTORY_VALIDATED_NOT_PARITY`; this is accounting evidence, not a
route port, runtime proof, or completion claim.

## C. QUEUED — operator decisions (only the human can do these)

| # | Action | Where |
| --- | --- | --- |
| C1 | Enable GitHub Actions (Settings/Billing) → CI turns green | github.com |
| C2 | Run `scripts/mac-revoke-tcc.sh` on mac-mini-m2; record date | `MAC_TCC_PERMISSIONS.md` |
| C3 | Provision `DATABASE_URL`, `CONTROL_API_TOKEN`, `BRAIN_SYNC_TOKEN` in host secret stores | never in repo |
| C4 | Choose Cloudflare tunnel strategy; rotate ghost-claw-os keystore | `NEXT_ACTIONS.md`, `INTEGRATION_MAP.md` |
| C5 | Open gates with tickets in order: deploy → cloudflare_dns → telegram_send → customer_messaging (adaptive_sync independent) | `GO_LIVE_GATE_CHECKLIST.md` |

## D. Standing constraints

Gates hold-by-default · no secrets in repo · one import per phase ·
proposal-only writes to Obsidian sources · consent-safe analytics only ·
brand-safe Thaimart copy · every feature ships with tests · Truth
Protocol on every report · Human ตัดสินใจสุดท้าย.

## E. Cadence

Each work session: pick the top unblocked B-item → run the method in
`.claude/skills/sirinx-master-plan/SKILL.md` → land it → update section
A/B here in the same commit.
