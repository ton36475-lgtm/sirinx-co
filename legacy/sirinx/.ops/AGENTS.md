Read `../AGENTS.md` first.

Scope: audits, contracts, manifests, continuation evidence.

Rules:
- validator is the authority on schema/path/file checks
- manifest files must stay machine-readable
- do not mark a task complete if `handoff_ready` is false
- preserve audit evidence for every material continuation step
