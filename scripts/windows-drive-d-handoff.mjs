import { mkdirSync, writeFileSync } from "node:fs";
import { join } from "node:path";

const root = process.cwd();
const out = join(root, "exports", "drive-d", "SIRINX_OS_PC_NODE_HANDOFF");
mkdirSync(out, { recursive: true });

const files = {
  "README.md": `# SIRINX OS PC Node Handoff

Generated by: npm run sync:handoff

Target Windows folder:

\`\`\`text
D:\\SIRINX_OS
\`\`\`

This handoff does not contain secrets. Use it to prepare the Windows PC worker node before any real AdaptiveSync execution.

## Steps

1. Create \`D:\\SIRINX_OS\` using \`setup-drive-d.ps1\`.
2. Share \`D:\\SIRINX_OS\` to the Mac mini on the LAN when ready.
3. Mount the share on the Mac as \`/Volumes/Windows-D\`.
4. Set \`SIRINX_WINDOWS_D_MOUNT=/Volumes/Windows-D/SIRINX_OS/sirinx-co\`.
5. Run \`npm run sync:plan\`.
6. Review dry-run output before any real copy.
`,
  "setup-drive-d.ps1": `$ErrorActionPreference = "Stop"

$Root = "D:\\SIRINX_OS"
$Folders = @(
  "$Root\\sirinx-co",
  "$Root\\mirrors",
  "$Root\\backups",
  "$Root\\logs",
  "$Root\\handoff"
)

foreach ($Folder in $Folders) {
  New-Item -ItemType Directory -Force -Path $Folder | Out-Null
}

Write-Host "SIRINX OS Windows Drive D folders are ready at $Root"
`,
  "node-heartbeat.example.json": JSON.stringify(
    {
      nodeId: "windows-worker-node",
      nodeType: "worker",
      hostname: "WINDOWS-PC",
      status: "healthy",
      capabilities: ["gpu-jobs", "media-dry-run", "backup-worker", "mysql-replica-planned"],
      blockedCapabilities: ["cloudflare-write", "production-db-write", "customer-send"],
      currentJobs: [],
      queueDepth: 0,
      lastCheckedAt: "2026-05-12T00:00:00+07:00",
      version: "pr-mono-002-adaptive-sync-telegram",
    },
    null,
    2,
  ),
  "telegram-control.example.env": `# Place real values only on the machine, never in git.
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
TELEGRAM_OWNER_IDS=
SIRINX_TELEGRAM_CONFIRM=
`,
};

for (const [name, content] of Object.entries(files)) {
  writeFileSync(join(out, name), `${content.trimEnd()}\n`);
}

console.log(`Drive D handoff generated at ${out}`);
